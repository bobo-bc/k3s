apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wikijs
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/bobo-bc/k3s
    targetRevision: main
    path: argo/apps/wikijs
  destination:
    server: https://kubernetes.default.svc
    namespace: media
  syncOptions:
  - CreateNamespace=true
  syncPolicy:
    automated:
      prune: true
      selfHeal: true


