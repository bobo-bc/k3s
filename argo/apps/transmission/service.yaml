apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: transmission-vpn
  name: transmission-vpn
  namespace: media
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9091
      targetPort: 9091
      protocol: TCP
  selector:
    app.kubernetes.io/name: transmission-vpn

