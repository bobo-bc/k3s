apiVersion: v1
kind: Service
metadata:
  labels:
    homepage.name: "transmission-vpn"
  name: transmission-vpn
  namespace: media
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 9091
      protocol: TCP
  selector:
    app.kubernetes.io/name: transmission-vpn

