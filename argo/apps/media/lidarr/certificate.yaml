apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: music-smeyer-ca
  namespace: media
spec:
  secretName: music-smeyer-ca-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - "*.smeyer.ca"
