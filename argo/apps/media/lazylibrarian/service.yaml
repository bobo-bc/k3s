# apps/sonarr/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: calibre
  namespace: media
spec:
  selector:
    app: calibre
  ports:
    - name: http
      port: 80
      targetPort: 8080
