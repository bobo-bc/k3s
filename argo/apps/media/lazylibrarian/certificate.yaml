apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: library-smeyer-ca
  namespace: media
spec:
  secretName: wildcard-smeyer-ca-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - "*.smeyer.ca"
