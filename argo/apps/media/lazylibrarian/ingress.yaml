# apps/sonarr/ingressroute.yaml
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: calibre
  namespace: media
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`library.smeyer.ca`)
      kind: Rule
      services:
        - name: calibre
          port: 80
  tls:
    certResolver: letsencrypt
