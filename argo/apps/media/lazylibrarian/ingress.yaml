    # Middleware: redirect HTTP -> HTTPS
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: calibre-redirect-https
  namespace: media
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
# IngressRoute for HTTPS
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: calibre
  namespace: media
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`library.smeyer.ca`)
      kind: Rule
      services:
        - name: calibre
          port: 80
  tls:
    certResolver: letsencrypt
---
# IngressRoute for HTTP (only used for redirect)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: calibre-http
  namespace: media
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`library.smeyer.ca`)
      kind: Rule
      middlewares:
        - name: calibre-redirect-https
      services:
        - name: calibre
          port: 80
      
