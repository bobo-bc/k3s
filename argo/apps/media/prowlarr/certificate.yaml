apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: index-smeyer-ca
  namespace: media
spec:
  secretName: index-smeyer-ca-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - "*.smeyer.ca"
