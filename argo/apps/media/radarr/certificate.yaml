apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: movies-smeyer-ca
  namespace: media
spec:
  secretName: movies-smeyer-ca-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - "*.smeyer.ca"
