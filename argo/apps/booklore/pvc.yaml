apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: booklore-pvc
  namespace: booklore
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
