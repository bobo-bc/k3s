apiVersion: v1
kind: Service
metadata:
  name: booklore
  namespace: booklore
spec:
  type: ClusterIP
  selector:
    app: booklore
  ports:
    - name: http
      port: 80          # Service port (what Traefik talks to)
      targetPort: 8080  # Container port (Booklore's internal port)
