apiVersion: v1
kind: PersistentVolume
metadata:
  name: booklore-books-pv
spec:
  capacity:
    storage: 200Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: 10.0.0.5  # <-- change to your NAS IP
    path: /mnt/data/media/books
