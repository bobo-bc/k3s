apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: booklore-books-pvc
  namespace: booklore
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi
  volumeName: booklore-books-pv
