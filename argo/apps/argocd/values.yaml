configs:
  cm:
    timeout.reconciliation: 30s
    admin.enabled: true

server:
  service:
    type: LoadBalancer
    loadBalancerIP: 10.0.0.34

  ingress:
    enabled: false

controller:
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi
