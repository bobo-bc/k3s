apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: smeyer-ca-wildcard
  namespace: default
spec:
  secretName: smeyer-ca-tls
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  dnsNames:
  - smeyer.ca
  - '*.smeyer.ca'
