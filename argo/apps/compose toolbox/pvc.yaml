# Persistent Storage using Longhorn
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: composetoolbox-data
  namespace: tools
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
