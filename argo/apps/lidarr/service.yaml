apiVersion: v1
kind: Service
metadata:
  labels:
    homepage.name: "lidarr"
  name: lidarr
  namespace: media
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8686
      protocol: TCP
  selector:
    app.kubernetes.io/name: lidarr
    app.kubernetes.io/instance: lidarr
