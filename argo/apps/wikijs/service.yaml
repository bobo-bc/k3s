---
apiVersion: v1
kind: Service
metadata:
  name: wikijs-service
  namespace: post
  labels:
    app.kubernetes.io/name: wikijs
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app.kubernetes.io/name: wikijs
