# wikijs-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wikijs-data
  namespace: wikijs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
