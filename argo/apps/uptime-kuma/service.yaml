apiVersion: v1
kind: Service
metadata:
  name: uptime-kuma
  namespace: tools
spec:
  selector:
    app: uptime-kuma
  ports:
    - name: http
      port: 80        # Service port Traefik uses
      targetPort: 3001 # Container port

