apiVersion: v1
kind: Service
metadata:
  labels:
    homepage.name: "uptime-kuma"
  name: uptime-kuma
  namespace: monitoring
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3001
      protocol: TCP
  selector:
    app.kubernetes.io/name: uptime-kuma

