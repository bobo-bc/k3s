apiVersion: v1
kind: Service
metadata:
  name: kavita
  namespace: media
  labels:
    homepage.name: "sonarr"
    app.kubernetes.io/name: kavita
    app.kubernetes.io/instance: kavita
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
  selector:
    app.kubernetes.io/name: kavita
    app.kubernetes.io/instance: kavita
