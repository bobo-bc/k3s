apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homepage-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn  # change if needed
