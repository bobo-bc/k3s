apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: homepage

resources:
  - serviceaccount.yaml
  - secret.yaml
  - clusterRole.yaml
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: homepage
    files:
      - settings.yaml=apps/config/settings.yaml
      - services.yaml=apps/config/services.yaml
      - bookmarks.yaml=apps/config/bookmarks.yaml
      - widgets.yaml=apps/config/widgets.yaml
      - kubernetes.yaml=apps/config/kubernetes.yaml
generatorOptions:
  disableNameSuffixHash: true
