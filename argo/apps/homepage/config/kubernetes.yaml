kubernetes:
  mode: cluster
  serviceAccount: homepage
  namespace_selector:
    - media
    - homepage
    - kube-system  # optional, if you want dashboard or metrics
    - tools
    - default
    - homepage
