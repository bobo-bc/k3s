apiVersion: v1
kind: Service
metadata:
  labels:
    homepage.name: "sonarr"
  name: sonarr
  namespace: media
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8989
      protocol: TCP
  selector:
    app.kubernetes.io/name: sonarr

