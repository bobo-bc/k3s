apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ghost-config
  namespace: ghost
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: nfs-client
