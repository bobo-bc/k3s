apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: post
spec:
  type: ClusterIP
  clusterIP: None  # for headless service, useful if you ever move to StatefulSet
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
