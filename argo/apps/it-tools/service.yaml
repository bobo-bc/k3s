apiVersion: v1
kind: Service
metadata:
  name: it-tools-service
  namespace: tools
  labels:
    app.kubernetes.io/name: it-tools
spec:
  type: LoadBalancer
  loadBalancerIP: 10.0.0.23   # MetalLB IP
  selector:
    app.kubernetes.io/name: it-tools
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
