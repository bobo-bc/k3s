apiVersion: v1
kind: Service
metadata:
  name: ittools-service
  namespace: tools
  labels:
    app.kubernetes.io/name: ittools
spec:
  type: LoadBalancer
  loadBalancerIP: 10.0.0.23   # MetalLB IP
  selector:
    app.kubernetes.io/name: ittools
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
