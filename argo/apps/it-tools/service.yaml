apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: it-tools
  name: it-tools
  namespace: tools
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app.kubernetes.io/name: it-tools
