apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: portainer-smeyer-ca
  namespace: portainer
spec:
  secretName: portainer-smeyer-ca-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - "*.smeyer.ca"
