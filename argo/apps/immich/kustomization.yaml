apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: immich

resources:
  - namespace.yaml
  - deployment.yaml
  - redis.yaml
  - mls.yaml
  - postgres.yaml
  - immich-config.yaml
  - pvc.yaml
