apiVersion: v1
kind: Service
metadata:
  name: drawio
  namespace: tools
  labels:
    homepage.name: "drawio"
    app.kubernetes.io/name: drawio
    app.kubernetes.io/instance: drawio
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: drawio
    app.kubernetes.io/instance: drawio
