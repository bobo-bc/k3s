apiVersion: v1
kind: Service
metadata:
  labels:
    app: "drawio"
  name: drawio-service
  namespace: tools
spec:
  selector:
    app: drawio
  ports:
    - name: http
      port: 80
      targetPort: 8080
