apiVersion: v1
kind: Service
metadata:
  name: drawio
  namespace: tools
  labels:
    app: drawio
spec:
  type: LoadBalancer
  loadBalancerIP: 10.0.0.31
  selector:
    app: drawio
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
