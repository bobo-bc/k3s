apiVersion: v1
kind: Service
metadata:
  name: wiki-postgres
  namespace: media
  labels:
    app: wiki-postgres
spec:
  selector:
    app: wiki-postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP
