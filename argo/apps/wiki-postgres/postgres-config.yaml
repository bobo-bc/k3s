apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-pvc-wikijs-postgres
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn
