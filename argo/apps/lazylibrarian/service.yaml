apiVersion: v1
kind: Service
metadata:
  name: lazylibrarian-service
  namespace: media
  labels:
    app.kubernetes.io/name: lazylibrarian
spec:
  type: ClusterIP
  ports:
    - port: 5299
      targetPort: 5299
      protocol: TCP
  selector:
    app.kubernetes.io/name: lazylibrarian
