apiVersion: v1
kind: Service
metadata:
  name: lazylibrarian
  namespace: media
  labels:
    homepage.name: "lazylibrarian"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5299
      protocol: TCP
  selector:
    app.kubernetes.io/name: lazylibrarian
