---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: radarr-ingress
  namespace: media
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.middlewares: media-radarr-redirect-https@kubernetescrd
spec:
  tls:
    - hosts:
        - radarr.lan
      secretName: wildcard-lan-tls
  rules:
    - host: radarr.lan
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: radarr-service
                port:
                  number: 7878
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: radarr-redirect-https
  namespace: media
spec:
  redirectScheme:
    scheme: https
    permanent: true
