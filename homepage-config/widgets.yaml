# widgets.yaml
- datetime:
    text_size: xl
    format:
      timeStyle: short
- kubernetes:
    cluster:
      show: true
    nodes:
      show: true
    resources:
      show: true
    kubeconfig: /app/config/kubeconfig.yaml  # Optional if running in-cluster
